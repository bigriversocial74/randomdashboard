<?php if(data_is_production()&&!performance_tables_ready()): ?><div class="flash warning" role="status"><strong>Production supplier-performance saving is not active yet.</strong> Import <code>database/20260726_section15_supplier_performance_improvement.sql</code> after Sections 11–14 during the final deployment window. Analysis remains available.</div><?php endif; ?>
<?php if(!$supplier): ?><section class="panel"><?php render_empty_state('No suppliers available','Add an in-scope supplier before creating a continuous-improvement review.'); ?></section><?php return; endif; ?>
<?php
$review=$selected??[
'id'=>null,'review_number'=>'Unsaved review','company_id'=>$blueprint['company_id']??null,'supplier_id'=>(int)$supplier['id'],'source_execution_id'=>(int)($execution['id']??0)?:null,'source_verification_id'=>(int)($blueprint['verification']['id']??0)?:null,'review_window_days'=>$blueprint['window_days']??30,'period_start'=>date('Y-m-d',strtotime('-30 days')),'period_end'=>date('Y-m-d'),'owner_id'=>(int)current_user()['id'],'reviewer_id'=>(int)current_user()['id'],'status'=>'draft','recommendation'=>$blueprint['recommendation']??'approved','risk_tier'=>$blueprint['risk_tier']??'medium','approval_id'=>null,'baseline'=>$blueprint['baseline']??[],'current'=>$blueprint['current']??[],'targets'=>$blueprint['targets']??[],'overall_score'=>$metrics['current_score']??0,'improvement_pct'=>$metrics['improvement_pct']??0,'sustainability_pct'=>$metrics['sustainability_pct']??0,'repeated_failure_count'=>0,'spend_exposure'=>$blueprint['spend_exposure']??0,'savings_retained'=>$blueprint['savings_retained']??0,'evidence_note'=>$blueprint['evidence_note']??'','next_review_date'=>date('Y-m-d',strtotime('+30 days')),'created_at'=>date('Y-m-d H:i:s'),'updated_at'=>date('Y-m-d H:i:s')];
$status=performance_effective_status($review);$readiness=$selected?performance_close_readiness($review,$actions):['ready'=>false,'open_actions'=>[],'metrics'=>$metrics];
$kpiLabels=['on_time_delivery'=>'On-time delivery','quality'=>'Quality','responsiveness'=>'Responsiveness','cost_competitiveness'=>'Cost competitiveness','fill_rate'=>'Fill rate','service_level'=>'Service level','lead_time_variance_days'=>'Lead-time variance','defect_rate_pct'=>'Defect rate','price_variance_pct'=>'Price variance','corrective_closure_pct'=>'Corrective closure'];
?>
<section class="performance-hero">
<div><span class="eyebrow">Supplier Performance Monitoring &amp; Continuous Improvement</span><h2><?= h($supplier['name']) ?></h2><p><?= h($review['evidence_note']) ?></p><div class="company-chip-row"><span><?= h($review['review_number']) ?></span><span><?= h(status_label($status)) ?></span><span><?= (int)$review['review_window_days'] ?>-day review</span><span><?= h(status_label($metrics['recommendation']??$review['recommendation'])) ?> recommendation</span><span><?= h(current_scope_label()) ?></span></div></div>
<div class="performance-score <?= h(status_class($metrics['risk_tier']??$review['risk_tier'])) ?>"><span>Current performance</span><strong><?= number_format((float)($metrics['current_score']??0),1) ?></strong><small><?= h(status_label($metrics['risk_tier']??$review['risk_tier'])) ?> risk · <?= number_format((float)($metrics['sustainability_pct']??0),1) ?>% sustainable</small></div>
</section>

<section class="performance-metrics" aria-label="Supplier performance summary">
<article><span>Improvement</span><strong><?= number_format((float)($metrics['improvement_pct']??0),1) ?>%</strong><small>Against the review baseline</small></article>
<article><span>Recovery sustainability</span><strong><?= number_format((float)($metrics['sustainability_pct']??0),1) ?>%</strong><small>Current results against targets</small></article>
<article><span>Regressions</span><strong><?= (int)($metrics['regression_count']??0) ?></strong><small>Threshold or baseline breaches</small></article>
<article><span>Corrective closure</span><strong><?= number_format((float)($metrics['corrective_action_closure_pct']??100),1) ?>%</strong><small><?= (int)($metrics['corrective_action_closed']??0) ?> of <?= (int)($metrics['corrective_action_count']??0) ?> closed</small></article>
<article><span>Spend exposure</span><strong><?= compact_money($metrics['spend_exposure']??0) ?></strong><small>Annual supplier spend monitored</small></article>
<article><span>Savings retained</span><strong><?= compact_money($metrics['savings_retained']??0) ?></strong><small>Verified recovery still protected</small></article>
</section>

<div class="performance-layout"><main>
<section class="panel"><header class="panel-head"><div><span class="eyebrow">KPI trend control</span><h2>Baseline, current performance, and approved targets</h2><p>Higher is better except lead-time variance, defect rate, and price variance.</p></div><span class="panel-meta"><?= (int)($metrics['regression_count']??0) ?> active regressions</span></header>
<div class="table-wrap"><table><thead><tr><th>Metric</th><th>Baseline</th><th>Current</th><th>Target</th><th>Movement</th></tr></thead><tbody><?php foreach($kpiLabels as $key=>$label): $base=(float)($review['baseline'][$key]??0);$now=(float)($review['current'][$key]??0);$target=(float)($review['targets'][$key]??0);$lower=in_array($key,['lead_time_variance_days','defect_rate_pct','price_variance_pct'],true);$good=$lower?$now<=$base:$now>=$base;$suffix=$key==='lead_time_variance_days'?' days':'%'; ?><tr><td><strong><?= h($label) ?></strong></td><td><?= number_format($base,1).$suffix ?></td><td><?= number_format($now,1).$suffix ?></td><td><?= number_format($target,1).$suffix ?></td><td><?= badge($good?'improving':'regressing') ?></td></tr><?php endforeach; ?></tbody></table></div></section>

<section class="panel"><header class="panel-head"><div><span class="eyebrow">Supplier corrective action</span><h2>Root cause, ownership, and verified closure</h2><p>Corrective actions remain open until target evidence is independently verified or the action is cancelled with evidence.</p></div><span class="panel-meta"><?= count($actions) ?> actions · <?= (int)($metrics['blocked_actions']??0) ?> blocked</span></header>
<?php foreach($actions as $row): ?><article class="corrective-card"><div class="corrective-head"><div><span class="eyebrow"><?= h($row['action_number']) ?> · <?= h(status_label($row['severity'])) ?></span><h3><?= h($row['title']) ?></h3><p><strong>Root cause:</strong> <?= h($row['root_cause']) ?></p><p><strong>Corrective action:</strong> <?= h($row['corrective_action']) ?></p></div><?= badge((string)$row['status']) ?></div><div class="corrective-kpis"><span>Owner #<?= (int)$row['owner_id'] ?></span><span>Due <?= h(date_us($row['due_date'])) ?></span><span><?= h(status_label($row['target_metric'])) ?> <?= number_format((float)$row['actual_value'],1) ?> / <?= number_format((float)$row['target_value'],1) ?></span></div><p class="evidence-note"><?= h($row['evidence_note']) ?></p>
<?php if(can('scorecards.create')): ?><form class="corrective-update" action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="save_corrective_action"><input type="hidden" name="id" value="<?= (int)$row['id'] ?>"><input type="hidden" name="review_id" value="<?= (int)$review['id'] ?>"><input type="hidden" name="title" value="<?= h($row['title']) ?>"><input type="hidden" name="root_cause" value="<?= h($row['root_cause']) ?>"><input type="hidden" name="corrective_action" value="<?= h($row['corrective_action']) ?>"><input type="hidden" name="target_metric" value="<?= h($row['target_metric']) ?>"><input type="hidden" name="target_value" value="<?= h((string)$row['target_value']) ?>"><label><span>Status</span><select name="status"><?php foreach(performance_action_statuses() as $value): ?><option value="<?= h($value) ?>" <?= $row['status']===$value?'selected':'' ?>><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Severity</span><select name="severity"><?php foreach(['low','medium','high','critical'] as $value): ?><option value="<?= h($value) ?>" <?= $row['severity']===$value?'selected':'' ?>><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Owner</span><select name="owner_id"><?php foreach(data_admin_visible_users() as $user): ?><option value="<?= (int)$user['id'] ?>" <?= (int)$row['owner_id']===(int)$user['id']?'selected':'' ?>><?= h($user['name']) ?></option><?php endforeach; ?></select></label><label><span>Due</span><input type="date" name="due_date" value="<?= h($row['due_date']) ?>" required></label><label><span>Actual value</span><input type="number" name="actual_value" step="0.1" value="<?= h((string)$row['actual_value']) ?>"></label><label class="span-2"><span>Evidence</span><textarea name="evidence_note" rows="2" required><?= h($row['evidence_note']) ?></textarea></label><button class="mini-button primary" type="submit">Update action</button></form><?php endif; ?></article><?php endforeach; ?>
<?php if(!$actions)render_empty_state('No corrective actions','Create corrective actions when a metric regresses, a threshold is breached, or supplier recovery is not sustained.'); ?>
</section>

<section class="panel"><header class="panel-head"><div><span class="eyebrow">Closed-loop intelligence</span><h2>Feedback into sourcing, scenarios, mitigation, and execution</h2><p>The same evidence adjusts downstream decision confidence without silently changing approved source records.</p></div></header><div class="feedback-grid"><article><span>Section 11 sourcing score</span><strong><?= number_format((float)($metrics['sourcing_score']??0),1) ?></strong><small>Use as current supplier-performance evidence</small></article><article><span>Section 12 risk adjustment</span><strong><?= number_format((float)($metrics['scenario_risk_adjustment']??0),1) ?> pts</strong><small>Increase or reduce disruption assumptions</small></article><article><span>Section 13 readiness</span><strong><?= number_format((float)($metrics['mitigation_readiness_pct']??0),1) ?>%</strong><small>Corrective closure and target attainment</small></article><article><span>Section 14 target confidence</span><strong><?= number_format((float)($metrics['execution_target_confidence_pct']??0),1) ?>%</strong><small>Confidence in future execution targets</small></article></div></section>

<section class="panel"><header class="panel-head"><div><span class="eyebrow">Performance evidence trail</span><h2>Regression and improvement events</h2></div><span class="panel-meta"><?= count($events) ?> events</span></header><div class="event-list"><?php foreach($events as $event): ?><article><div><?= badge((string)$event['severity']) ?><strong><?= h(status_label($event['event_type'])) ?></strong><?php if($event['metric']): ?><span><?= h(status_label($event['metric'])) ?></span><?php endif; ?></div><p><?= h($event['evidence_note']) ?></p><small><?= h(date_us($event['created_at'])) ?> · <?= h((string)($event['previous_value']??'—')) ?> → <?= h((string)($event['current_value']??'—')) ?> · threshold <?= h((string)($event['threshold_value']??'—')) ?></small></article><?php endforeach; ?><?php if(!$events)render_empty_state('No events recorded','Save a review or corrective action to begin the immutable performance trail.'); ?></div></section>
</main><aside>
<article class="performance-side-card"><span class="eyebrow">Governance</span><h2>Review lifecycle</h2><div class="status-callout"><span>Current state</span><strong><?= h(status_label($status)) ?></strong></div><p><?= performance_requires_approval($review,$metrics)?'This recommendation requires human approval because of risk, supplier status, or spend exposure.':'This lower-risk review can enter monitoring without separate approval.' ?></p><?php if($selected): ?><div class="side-actions"><?php if(can('approvals.submit')&&in_array($status,['draft','changes_requested'],true)&&performance_requires_approval($review,$metrics)): ?><form action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="submit"><input type="hidden" name="id" value="<?= (int)$review['id'] ?>"><button class="button primary" type="submit">Route approval</button></form><?php endif; ?><?php if(can('scorecards.create')&&in_array($status,['draft','approved','changes_requested'],true)): ?><form action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="start_monitoring"><input type="hidden" name="id" value="<?= (int)$review['id'] ?>"><button class="button secondary" type="submit">Start monitoring</button></form><?php endif; ?><?php if(can('scorecards.create')&&$status==='monitoring'): ?><form action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="close_review"><input type="hidden" name="id" value="<?= (int)$review['id'] ?>"><button class="button ghost" type="submit" <?= $readiness['ready']?'':'disabled' ?>>Close review</button></form><?php endif; ?></div><?php endif; ?></article>
<article class="performance-side-card"><span class="eyebrow">Saved reviews</span><h2>Monitoring history</h2><?php foreach(performance_reviews() as $row): $rowMetrics=performance_metrics($row,performance_actions((int)$row['id'])); ?><div class="review-history-row"><strong><?= h(data_supplier_name((int)$row['supplier_id'])) ?></strong><small><?= h($row['review_number']) ?> · <?= (int)$row['review_window_days'] ?> days · <?= number_format((float)$rowMetrics['sustainability_pct'],1) ?>% sustainable</small><div><?= badge(performance_effective_status($row)) ?><a href="<?= h(app_url('performance.php?id='.(int)$row['id'])) ?>">Open</a></div></div><?php endforeach; ?><?php if(!performance_reviews())render_empty_state('No performance reviews','Create a 30-, 60-, or 90-day supplier review.'); ?></article>
</aside></div>

<?php if(can('scorecards.create')): ?><section class="panel"><header class="panel-head"><div><span class="eyebrow">Performance review builder</span><h2><?= $selected?'Update review':'Create review' ?></h2><p>Capture current evidence and approved targets. Baseline values remain frozen after the first save.</p></div><span class="panel-meta">30 / 60 / 90-day review</span></header><form class="stacked-form" action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="save_review"><input type="hidden" name="id" value="<?= (int)($review['id']??0) ?>"><div class="form-grid"><label><span>Supplier</span><select name="supplier_id" required><?php foreach(data_visible_collection('suppliers') as $row): ?><option value="<?= (int)$row['id'] ?>" <?= (int)$review['supplier_id']===(int)$row['id']?'selected':'' ?>><?= h($row['name']) ?></option><?php endforeach; ?></select></label><label><span>Review window</span><select name="review_window_days"><?php foreach([30,60,90] as $days): ?><option value="<?= $days ?>" <?= (int)$review['review_window_days']===$days?'selected':'' ?>><?= $days ?> days</option><?php endforeach; ?></select></label><label><span>Period start</span><input type="date" name="period_start" value="<?= h($review['period_start']) ?>" required></label><label><span>Period end</span><input type="date" name="period_end" value="<?= h($review['period_end']) ?>" required></label><label><span>Owner</span><select name="owner_id"><?php foreach(data_admin_visible_users() as $user): ?><option value="<?= (int)$user['id'] ?>" <?= (int)$review['owner_id']===(int)$user['id']?'selected':'' ?>><?= h($user['name']) ?></option><?php endforeach; ?></select></label><label><span>Reviewer</span><select name="reviewer_id"><?php foreach(data_admin_visible_users() as $user): ?><option value="<?= (int)$user['id'] ?>" <?= (int)$review['reviewer_id']===(int)$user['id']?'selected':'' ?>><?= h($user['name']) ?></option><?php endforeach; ?></select></label><label><span>Recommendation</span><select name="recommendation"><?php foreach(performance_recommendations() as $value): ?><option value="<?= h($value) ?>" <?= $review['recommendation']===$value?'selected':'' ?>><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Risk tier</span><select name="risk_tier"><?php foreach(performance_risk_tiers() as $value): ?><option value="<?= h($value) ?>" <?= $review['risk_tier']===$value?'selected':'' ?>><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Repeated failures</span><input type="number" min="0" name="repeated_failure_count" value="<?= (int)$review['repeated_failure_count'] ?>"></label><label><span>Next review</span><input type="date" name="next_review_date" value="<?= h($review['next_review_date']) ?>" required></label><label><span>Spend exposure</span><input type="number" min="0" step="0.01" name="spend_exposure" value="<?= h((string)$review['spend_exposure']) ?>"></label><label><span>Savings retained</span><input type="number" min="0" step="0.01" name="savings_retained" value="<?= h((string)$review['savings_retained']) ?>"></label><label class="span-2"><span>Evidence note</span><textarea name="evidence_note" rows="4" required><?= h($review['evidence_note']) ?></textarea></label></div>
<div class="metric-editor-grid"><?php foreach($kpiLabels as $key=>$label): ?><article><span><?= h($label) ?></span><label><small>Current</small><input type="number" step="0.1" name="current_<?= h($key) ?>" value="<?= h((string)($review['current'][$key]??0)) ?>"></label><label><small>Target</small><input type="number" step="0.1" name="target_<?= h($key) ?>" value="<?= h((string)($review['targets'][$key]??0)) ?>"></label><?php if(!$selected): ?><input type="hidden" name="baseline_<?= h($key) ?>" value="<?= h((string)($review['baseline'][$key]??0)) ?>"><?php endif; ?></article><?php endforeach; ?></div><div class="form-actions"><button class="button primary" type="submit">Save performance review</button></div></form></section>
<?php if($selected): ?><section class="panel"><header class="panel-head"><div><span class="eyebrow">New corrective action</span><h2>Create supplier improvement commitment</h2></div></header><form class="form-grid" action="<?= h(app_url('performance-action.php')) ?>" method="post"><?= csrf_field() ?><input type="hidden" name="action" value="save_corrective_action"><input type="hidden" name="id" value="0"><input type="hidden" name="review_id" value="<?= (int)$review['id'] ?>"><label class="span-2"><span>Title</span><input name="title" maxlength="190" required></label><label class="span-2"><span>Root cause</span><textarea name="root_cause" rows="3" required></textarea></label><label class="span-2"><span>Corrective action</span><textarea name="corrective_action" rows="3" required></textarea></label><label><span>Owner</span><select name="owner_id"><?php foreach(data_admin_visible_users() as $user): ?><option value="<?= (int)$user['id'] ?>"><?= h($user['name']) ?></option><?php endforeach; ?></select></label><label><span>Severity</span><select name="severity"><?php foreach(['low','medium','high','critical'] as $value): ?><option value="<?= h($value) ?>"><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Status</span><select name="status"><?php foreach(performance_action_statuses() as $value): ?><option value="<?= h($value) ?>"><?= h(status_label($value)) ?></option><?php endforeach; ?></select></label><label><span>Due date</span><input type="date" name="due_date" value="<?= h(date('Y-m-d',strtotime('+14 days'))) ?>" required></label><label><span>Target metric</span><select name="target_metric"><?php foreach($kpiLabels as $key=>$label): ?><option value="<?= h($key) ?>"><?= h($label) ?></option><?php endforeach; ?></select></label><label><span>Target value</span><input type="number" step="0.1" name="target_value" value="95"></label><label><span>Actual value</span><input type="number" step="0.1" name="actual_value" value="0"></label><label class="span-2"><span>Evidence</span><textarea name="evidence_note" rows="3" required></textarea></label><div class="span-2 form-actions"><button class="button primary" type="submit">Create corrective action</button></div></form></section><?php endif; ?><?php endif; ?>
