name: Bootstrap Phase 2 source (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Bootstrap disabled
        run: |
          echo 'Automatic source bootstrap is disabled.'
          echo 'The prior encoded archive payload was incomplete and must not be retried.'
          echo 'Push the validated Phase 2 source through a normal Git client before enabling CI.'
